# Tools

This directory is for local reverse-engineering helper tools.

`cfr.jar` is intentionally ignored and should be downloaded on fresh clones when
decompiler work is needed:

```cmd
curl.exe -L -o tools\cfr.jar https://www.benf.org/other/cfr/cfr-0.152.jar
```

